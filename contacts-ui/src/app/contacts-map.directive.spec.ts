import { ContactsMapDirective } from './contacts-map.directive';

describe('ContactsMapDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactsMapDirective();
    expect(directive).toBeTruthy();
  });
});
